source 'https://rubygems.org'

reek_version = ENV['REEK_VERSION']
gem 'reek', "~> #{reek_version}" if reek_version
# gem 'unparser', '0.2.4'

# Specify your gem's dependencies in guard-reek.gemspec
gemspec